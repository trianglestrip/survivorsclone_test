# Cursor Rules for SurvivorsClone_Test

## Godot 环境配置

### Godot 可执行文件路径
- **Godot 编辑器**: `F:\project\godot\Godot_v4.6.1-stable_win64.exe`
- **Godot 控制台**: `F:\project\godot\Godot_v4.6.1-stable_win64_console.exe`

### 测试命令
运行 headless 测试：
```powershell
F:\project\godot\Godot_v4.6.1-stable_win64_console.exe --headless --path . --script tests/test_xxx.gd
```

运行游戏：
```powershell
F:\project\godot\Godot_v4.6.1-stable_win64.exe --path .
```

## 项目架构原则（暖雪风格）

### 设计原则
1. **零硬编码**：所有数值在GameConstants或配置文件中
2. **基类继承**：清晰的继承体系（BaseAttack, BaseActiveSkill, BaseEnemy, StatusEffect）
3. **逻辑解耦**：数值、逻辑、特效完全分离
4. **高度封装**：重复代码封装为工具类（VisualEffectsHelper）
5. **配置驱动**：完全由JSON配置文件驱动

### 核心架构
- **GameConstants**：统一管理颜色、数值、枚举
- **VisualEffectsHelper**：封装所有特效逻辑（震动、闪烁、淡出）
- **StatusEffect**：状态效果基类（减速、灼烧、中毒、冻结）
- **BaseActiveSkill**：主动技能基类（QER技能）
- **BaseAttack**：攻击基类（MeleeAttack, RangedAttack）

### Player组件系统
- InputManager - 输入处理
- AttackManager - 双攻击模式（主攻击+副攻击）
- DashManager - 冲刺闪避
- ActiveSkillManager - QER技能管理
- SectManager - 宗派系统
- UpgradeManager - 升级管理（简化版）
- ExperienceManager - 经验系统
- PlayerStats - 属性管理

### 宗派系统（阶段2）
- 4大宗派：冰心宗、雷鸣宗、烈焰宗、毒瘴宗
- 每个宗派3个技能（Q/E/R）
- 配置文件：config/sect_config.json

### 配置文件
- stage1_controls.json - 操作和攻击配置
- sect_config.json - 宗派和技能配置
- upgrade_config.json - 升级配置（简化）

### 已删除的旧系统
- ❌ 旧被动技能系统（ice_spear, tornado, javelin）
- ❌ GPU实例化管理器（skill_instance_manager, enemy_instance_manager）
- ❌ 复杂的Effect工厂系统
- ❌ 旧注册系统（base_registry, skill_registry, enemy_registry）
- ❌ 旧敌人生成器（enemy_spawner_gpu, spawn_info）

## 编码规范

### GDScript 风格
- 使用 snake_case 命名变量和函数
- 使用 PascalCase 命名类和场景
- 添加类型提示以提高代码可读性

### 错误处理
- 使用 `push_error()` 报告严重错误
- 使用 `push_warning()` 报告警告
- 在关键路径添加验证检查

### 配置文件编码
- 所有 INI 文件必须使用 UTF-8 编码（无 BOM）
- 使用自定义 `ini_parser.gd` 处理编码问题

## 文档管理

### 文档原则
- **精简文档**：不要频繁创建总结性文档
- **重要信息记录到rules**：架构、设计原则、关键配置
- **保留必要文档**：
  - README.md - 项目说明
  - QUICK_REFERENCE.md - 快速参考
  - .trae/documents/ - 详细技术文档（仅必要时）

### 当前状态（2026-03-29）
- 提交：最新
- 阶段1：✅ 操作控制系统（100%）
- 阶段2：✅ 宗派系统（Q技能完成，E/R待实现）
- 阶段3：✅ 武器系统（100%）
- 敌人系统：✅ SimpleEnemy + SimpleEnemySpawner（基础实现）
- 伤害系统：✅ 完整伤害循环（玩家↔敌人双向伤害）
- 测试：✅ 伤害系统4/4，操作模拟9/9，敌人和特效3/3（100%）
- Q技能特效：✅ 自动清理（auto_projectile.gd, auto_fade_sprite.gd）
- E/R技能：框架完成，需实现具体脚本

### 技术要点
- **弹射物自动清理**：使用auto_projectile.gd脚本，在_process中检测距离和命中
- **特效自动淡出**：使用auto_fade_sprite.gd脚本，在_ready中启动fade_out协程
- **敌人生成**：SimpleEnemySpawner每2秒生成一个敌人，最多20个
- **伤害系统**：
  - 玩家HurtBox（碰撞层1，遮罩2）接收敌人攻击
  - 敌人HurtBox（碰撞层2，遮罩0）接收玩家攻击
  - 玩家攻击HitBox（碰撞层0，遮罩2）使用元数据传递伤害
  - 敌人攻击HitBox（碰撞层2，遮罩1）使用enemy_hit_box.gd脚本
  - hurt_box.gd支持属性和元数据两种方式获取伤害值
